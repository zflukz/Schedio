# ----------------------------------------------
# Schedio - Environment Variables (Example)
#
# Copy this file to `.env` in the project root
# and fill in values as needed.
#
# Notes:
# - With Docker Compose, the backend container connects to Postgres via the service name `db`.
# - If you run everything manually (no Docker), DB_url typically points to localhost.
# ----------------------------------------------

# ========== Database Configuration ==========
# Docker Compose (recommended):
DB_url=jdbc:postgresql://db:5432/schedio

# Manual setup alternative:
# DB_url=jdbc:postgresql://localhost:5432/schedio

DB_user=postgres
DB_password=postgres

# ========== JWT Configuration (Optional) ==========
# The backend also has defaults in application.properties; override here if desired.
JWT_SECRET=change_me_to_a_long_random_string_at_least_256_bits
JWT_EXPIRATION=86400000

# ========== Google OAuth2 (Optional) ==========
# Enable OAuth2 in the backend by setting:
# SPRING_PROFILES_ACTIVE=oauth2

GOOGLE_ID=
GOOGLE_SECRET=
OAUTH_REDIRECT_URI=http://localhost:8080/login/oauth2/code/google

# Spring profiles (optional)
SPRING_PROFILES_ACTIVE=

# ========== Frontend -> Backend API Base URL ==========
# Used by the React app (client/src/config/api.ts). Required when the backend host port is not 8080.
REACT_APP_API_BASE_URL=http://localhost:8080

# ========== CORS / Frontend URLs ==========
CORS_ALLOWED_ORIGINS=http://localhost:3000
FRONTEND_BASE_URL=http://localhost:3000
FRONTEND_HOME_URL=http://localhost:3000/home

# ========== Email (Optional: password reset) ==========
EMAIL_USERNAME=
EMAIL_PASSWORD=

# ========== Vercel Blob (Optional: file uploads) ==========
blob=
